package com.arsildo.merrpatenten.utils

const val ANIMATION_DURATION = 512

/*
@OptIn(ExperimentalFoundationApi::class)
suspend fun PagerState.previousPage() {
    if (canScrollBackward) animateScrollToPage(page = currentPage - 1)
}

@OptIn(ExperimentalFoundationApi::class)
suspend fun PagerState.nextPage() {
    if (canScrollForward) animateScrollToPage(page = currentPage + 1)
}*/
